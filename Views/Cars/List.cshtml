<h1>Все автомобили</h1>
<h2>@Model.CuurCategory</h2>
<div class="row mt-5 mb-2">
    @{
        foreach (Car Car in Model.AllCars) //глобальный обьект который передается через представление в контроллере viewModels
        {
            //  @Html.Partial("AllCars", Car)
            <partial name="AllCars" for="@Car" />
        }
    }
</div>